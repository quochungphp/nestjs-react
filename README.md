# viact-nestjs-react

# docker

```bash

docker-compose -f docker-compose.db.yaml up -d
docker-compose -f docker-compose.db.yaml down

```

# start project

```bash cd backend/
npm run build
npm run migration
npm run start:dev

```
